; Endless program that reads a character from stdin and then prints it.

; Increment value at pointer (0 -> 1)
arch

; Loop
the
    i   ; Increment pointer
    by  ; Read character from stdin into value at pointer
    btw ; Print value at pointer as character
    use ; Decrement pointer
way

; The loop never ends because value at pointer is always 1 when the loop check
; occurs
